#!/usr/bin/env volcano

from volcano.shell import sleep

count = 10
final_message = "We have liftoff"

# while count > 0:
#     print(f"T-minus {count}")
#     sleep(1)
#     count -= 1

# print(final_message)